<%= render 'shared/errors', obj: @category %>
<%= form_with(model: @category, class: "shadow-lg p-4 mb-3 bg-gray rounded", local:true, data: { turbo: false }, local:true ) do |f| %>

  <div class="input-group justify-content-center mb-3">
    <%= f.label :name, "Category name",  class:"col-2 col-form-label" %>
    <div class="col-10">
        <%= f.text_field :name, class:"form-control", placeholder: "Enter a name" %>
    </div>
  </div>

  <div class="input-group justify-content-center">
      <%= f.submit class:"btn btn-success btn-light" %>
  </div>
<% end %>
